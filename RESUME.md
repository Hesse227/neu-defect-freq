# 简历条目（基于真实实验数据，可直接使用）

## 版本 A：标准版（推荐，突出系统性与诚实结论）

> **基于频域特征增强的工业表面缺陷检测（个人项目，2026.08）**
> 针对 NEU-DET 钢材表面缺陷数据集（6 类 / 1800 图），基于 PyTorch/ultralytics 复现 YOLOv8n 检测基线（mAP@50 = 77.1%），受 DSF-Net 频域解耦思想启发，设计并集成 FcaNet 式 DCT 多谱通道注意力模块（参数量仅 +0.4%）；完成 5 组严格对照消融（含 SE 同容量对照组、双种子方差对照、门初始化消融），发现频域注意力使低频斑块类缺陷（rolled-in_scale）召回率提升 7 个点、且超出种子方差带，但对高频细密裂纹类（crazing）产生负迁移，整体 mAP 无显著增益——定位出静态频域重加权与动态频率响应（DSF-Net 路线）的本质差异；独立解决 AMP 混合精度与跨设备 checkpoint 加载两类工程问题并沉淀回归测试；开源全部训练/评估/可视化代码与实验记录。

## 版本 B：短版（简历空间紧张时）

> 复现并改进 YOLOv8n 工业缺陷检测（NEU-DET，6 类）：设计 DCT 多谱通道注意力模块（参数 +0.4%），完成含 SE 对照与双种子方差分析的 5 组消融，发现频域注意力对低频缺陷类召回 +7 点、对高频缺陷类负迁移的类别级效应，定量论证了动态频率响应的必要性；独立修复 AMP dtype 与跨设备权重加载问题，全流程（数据管线-训练-评估-可视化）开源。

## 版本 C：如果后续跑出正提升（留模板，数字必须真实替换）

> 基于频域特征增强的工业表面缺陷检测（个人项目，2026.08）：受 DSF-Net 频域解耦思想启发，在 YOLOv8n 中设计并集成 DCT 多谱通道注意力模块；在 NEU-DET（6类/1800图）上完成 baseline 复现与 5 组消融实验，mAP@50 达 __._%，较 baseline 提升 _._%，最难类别（crazing）AP 提升 _._%，参数量仅增 _%；开源代码与全部实验记录。

---

## 套磁邮件中的一句话版本

> 我近期复现了 DSF-Net 所处的工业缺陷检测设置：在 NEU-DET 上把 FcaNet 式 DCT 多谱通道注意力插入 YOLOv8n 并做了含 SE 对照、双种子方差分析的消融，发现静态频域重加权对低频缺陷类（rolled-in_scale，召回 +7 点）有超出方差带的真实增益、但对高频细密缺陷有负迁移——这让我更理解论文中动态频率响应函数要解决的问题。

## 面试要点（8 问速答）

1. **为什么 DCT 池化理论上优于 GAP？** GAP 等价于仅取 2D-DCT 的直流（0,0）分量，丢弃全部纹理/边缘频率证据；DCT 多谱池化每组通道保留不同低频分量，squeeze 阶段就携带频谱结构信息。
2. **模块插在哪、为什么？** backbone 输出 P3/P4/P5 的 C2f 之后（下采样路径和 FPN 横向连接都吃到重加权特征）；P3-only 是检验"小目标层收益最大"假设——实测 P3-only 整体反而更差，说明增益与插入深度不是简单关系。
3. **SE 对照组说明什么？** SE 与 DCT 容量完全一致（同 FC 门、同位置），若提升只来自"加了注意力"，两者应同幅提升；实测 SE 整体最低（0.723），排除"随便加注意力都有用"的解释，把效应锁定在 pooling 的频谱成分上。
4. **1800 张如何防过拟合？** COCO 预训练微调 + 默认增广（mosaic/HSV/scale）+ early stopping（patience=30）+ cosine lr + close_mosaic 最后 15 epoch；更重要是**双种子评估**——单种子 ±2.4 点方差下任何 <2.5 点的差异都不可信。
5. **mAP@50 vs mAP@50:95？** 前者只要求 IoU>0.5 命中，后者对定位精度加权平均；工业产线若只做"有无缺陷+分类"告警，mAP@50 更相关，若要引导修复/机械臂则需高 IoU 指标。
6. **与 DSF-Net 的差异？** DSF-Net 用可学习频率响应函数+双空间 Mixer 做输入自适应的频域解耦；我的版本是静态频基通道加权。我的消融恰好证明了静态版本的局限：频率重加权对不同缺陷类是零和的（低频类受益、高频类受损），这正是动态响应要解的问题。
7. **给你真实镀膜产线数据，下一步？** ① 域偏移：NEU 预训练→镀膜域微调/域自适应；② 频域先验：把 DCT 基换成数据驱动学出的频率响应（朝 DSF-Net 靠）；③ 工程约束：CPU/边缘端推理时延（本项目实测 GPU 240-340 FPS、CPU ~90ms/图）。
8. **多缺陷共存怎么处理？** NEU-DET 本身单图多框（1-9 框/图），YOLO 检测头天然并行预测多类多框；难点在类间相似与尺度跨度——P3/P4/P5 三尺度头的意义所在，消融也显示插入深度影响类间权衡。
